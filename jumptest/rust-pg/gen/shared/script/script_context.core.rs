// <auto-generated>
// Rust port of the ScriptContext portion of ScriptHost.core.cs.
//
// The execution context handed to a script: the triggering record
// (`transaction`), free-form `args`, the captured result, an optional error,
// and a return code (0 = success).
// </auto-generated>

use std::collections::HashMap;

use common::BaseObject;
use serde_json::Value;

#[derive(Debug, Clone, Default)]
pub struct ScriptContext {
    /// The record that triggered the script (read by scripts as `ctx`, and
    /// any mutations are written back here after execution).
    pub transaction: BaseObject,
    /// Free-form arguments exposed to the script as `args`.
    pub args: HashMap<String, Value>,
    /// The script's return value, if any.
    pub script_result: Option<Value>,
    /// Error message if the script failed.
    pub script_exception: Option<String>,
    /// 0 = success, >= 1 = error.
    pub ret_code: i32,
}

impl ScriptContext {
    pub fn new() -> Self {
        ScriptContext::default()
    }

    pub fn with_transaction(transaction: BaseObject) -> Self {
        ScriptContext { transaction, ..Default::default() }
    }
}
