// <auto-generated>
// Rust port of Execution.core.cs. Enumerated type for core.exec_status values.
// </auto-generated>

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ExecStatus {
    Idle = 1,
    Dispatched = 2,
    Executing = 3,
    Completed = 4,
    Failed = 5,
    Cancelled = 6,
    Timeout = 7,
    Interrupted = 8,
    Suspended = 9,
}

impl ExecStatus {
    pub fn description(self) -> &'static str {
        match self {
            ExecStatus::Idle => "Idle",
            ExecStatus::Dispatched => "Dispatched",
            ExecStatus::Executing => "Executing",
            ExecStatus::Completed => "Completed",
            ExecStatus::Failed => "Failed",
            ExecStatus::Cancelled => "Cancelled",
            ExecStatus::Timeout => "Timeout",
            ExecStatus::Interrupted => "Interrupted",
            ExecStatus::Suspended => "Suspended",
        }
    }

    pub fn from_description(description: &str) -> Option<Self> {
        match description.to_ascii_lowercase().as_str() {
            "idle" => Some(ExecStatus::Idle),
            "dispatched" => Some(ExecStatus::Dispatched),
            "executing" => Some(ExecStatus::Executing),
            "completed" => Some(ExecStatus::Completed),
            "failed" => Some(ExecStatus::Failed),
            "cancelled" => Some(ExecStatus::Cancelled),
            "timeout" => Some(ExecStatus::Timeout),
            "interrupted" => Some(ExecStatus::Interrupted),
            "suspended" => Some(ExecStatus::Suspended),
            _ => None,
        }
    }

    pub fn from_i64(value: i64) -> Option<Self> {
        match value {
            1 => Some(ExecStatus::Idle),
            2 => Some(ExecStatus::Dispatched),
            3 => Some(ExecStatus::Executing),
            4 => Some(ExecStatus::Completed),
            5 => Some(ExecStatus::Failed),
            6 => Some(ExecStatus::Cancelled),
            7 => Some(ExecStatus::Timeout),
            8 => Some(ExecStatus::Interrupted),
            9 => Some(ExecStatus::Suspended),
            _ => None,
        }
    }
}
