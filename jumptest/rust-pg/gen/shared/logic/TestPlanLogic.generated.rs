// <auto-generated> Rust port of templateLogic.generated.cs for `TestPlan`.
// Regenerated on every run. The CRUD operations are normal `pub(crate)` methods;
// `dispatch` is the generated string->method router; `exec` is the authorized
// entry point (runs the proxy hooks via LogicExec). Hand-written operations go
// in TestPlanLogic.user.rs (the `dispatch_user` hook). </auto-generated>

use common::*;
use domain::*;
use persist::*;
use serde_json::Value;

use crate::base_logic::LogicError;
use crate::logic_context::LogicContext;
use crate::logic_exec::{ExecProof, LogicDispatch, LogicExec};

pub struct TestPlanLogic;

impl TestPlanLogic {
    pub fn new() -> Self {
        TestPlanLogic
    }

    /// Authorized entry point: logging + authorization (+ events) then dispatch.
    pub fn exec(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call(&TestPlanLogic::new(), method, ctx)
    }

    /// Bypass the hooks/authorization. Use only where no principal exists
    /// (import/export, tests).
    pub fn exec_unchecked(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call_unchecked(&TestPlanLogic::new(), method, ctx)
    }

    // ── raw operations (reachable only via dispatch / LogicExec) ─────────

    pub(crate) fn select(&self) -> Result<Vec<TestPlan>, LogicError> {
        Logger::debug("Processing TestPlanLogic select");
        Ok(DBPersist::execute_query_into::<TestPlan>(
            "app.test_plan-select",
            "default",
        )?)
    }

    pub(crate) fn get(&self, id: i64) -> Result<Option<TestPlan>, LogicError> {
        Logger::debug(format!("Processing TestPlanLogic get ID={}", id));
        let sql = format!(
            "SELECT * FROM app.test_plan WHERE id = {} AND is_active = 1",
            id
        );
        Ok(DBPersist::select_into::<TestPlan>(&sql, "default")?
            .into_iter()
            .next())
    }

    pub(crate) fn view(&self, id: i64) -> Result<Option<TestPlanView>, LogicError> {
        Logger::debug(format!("Processing TestPlanLogic view ID={}", id));
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        Ok(DBPersist::execute_query_into_filtered::<TestPlanView>(
            "app.test_plan-get",
            &filter,
            "default",
        )?
        .into_iter()
        .next())
    }

    pub(crate) fn history(&self, id: i64) -> Result<Vec<TestPlanHistory>, LogicError> {
        let sql = format!(
            "SELECT * FROM app.test_plan WHERE id = {} ORDER BY txn_id DESC",
            id
        );
        Ok(DBPersist::select_into::<TestPlanHistory>(&sql, "default")?)
    }

    pub(crate) fn children(
        &self,
        id: i64,
        child_suffix: &str,
    ) -> Result<Vec<BaseObject>, LogicError> {
        let query_name = format!("app.test_plan-children-{}", child_suffix);
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        let rows = DBPersist::execute_query_rows_filtered(&query_name, &filter, "default")?;
        Ok(rows
            .into_iter()
            .map(|row| {
                let mut o = BaseObject::new();
                for (k, v) in row {
                    o.set(&k, v);
                }
                o
            })
            .collect())
    }

    pub(crate) fn insert(&self, testplan: &mut TestPlan) -> Result<(), LogicError> {
        Logger::debug("Processing TestPlanLogic insert");
        testplan.base.set("is_active", Value::from(1));
        DBPersist::insert(testplan, "default")?;
        Ok(())
    }

    pub(crate) fn update(&self, id: i64, testplan: &mut TestPlan) -> Result<(), LogicError> {
        Logger::debug(format!("Processing TestPlanLogic update ID={}", id));
        testplan.base.set("id", Value::from(id));
        DBPersist::update(testplan, "default")?;
        Ok(())
    }

    pub(crate) fn put(&self, testplan: &mut TestPlan) -> Result<(), LogicError> {
        Logger::debug("Processing TestPlanLogic put");
        testplan.base.set("is_active", Value::from(1));
        DBPersist::put(testplan, "default")?;
        Ok(())
    }

    pub(crate) fn delete(&self, id: i64) -> Result<(), LogicError> {
        if let Some(mut testplan) = self.get(id)? {
            testplan.base.set("is_active", Value::from(0));
            DBPersist::update(&mut testplan, "default")?;
        }
        Ok(())
    }
}

impl LogicDispatch for TestPlanLogic {
    const DOMAIN: &'static str = "TestPlan";

    fn dispatch(
        &self,
        method: &str,
        ctx: &mut LogicContext,
        _proof: &ExecProof,
    ) -> Result<Value, LogicError> {
        match method {
            "select" => {
                let rows = self.select()?;
                let cols = TestPlan::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.base.data, &cols)).collect(),
                ))
            }
            "get" => {
                let found = self.get(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.base.data, &TestPlan::columns()))
                    .unwrap_or(Value::Null))
            }
            "view" => {
                let found = self.view(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.inner.base.data, &TestPlan::columns()))
                    .unwrap_or(Value::Null))
            }
            "history" => {
                let rows = self.history(ctx.id())?;
                let cols = TestPlan::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.inner.base.data, &cols)).collect(),
                ))
            }
            "children" => {
                let rows = self.children(ctx.id(), &ctx.arg_str("child_suffix"))?;
                Ok(serde_json::to_value(rows.iter().map(|o| &o.data).collect::<Vec<_>>())
                    .unwrap_or(Value::Null))
            }
            "insert" => {
                let mut o = TestPlan::from_base(ctx.transaction.clone());
                self.insert(&mut o)?;
                let v = to_typed_json(&o.base.data, &TestPlan::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "update" => {
                let mut o = TestPlan::from_base(ctx.transaction.clone());
                self.update(ctx.id(), &mut o)?;
                let v = to_typed_json(&o.base.data, &TestPlan::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "put" => {
                let mut o = TestPlan::from_base(ctx.transaction.clone());
                self.put(&mut o)?;
                let v = to_typed_json(&o.base.data, &TestPlan::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "delete" => {
                self.delete(ctx.id())?;
                Ok(Value::Null)
            }
            "enum" => {
                // id + display-string pairs for dropdowns (matches the .NET
                // /api/<obj>/enum endpoint).
                let rows = self.select()?;
                let enums: Vec<EnumHelper> = rows
                    .iter()
                    .map(|o| EnumHelper::new(o.id(), o.base.get_rwk_string(false)))
                    .collect();
                Ok(serde_json::to_value(&enums).unwrap_or(Value::Null))
            }
            // Custom operations: add an arm in the user file's `dispatch_user`
            // and delegate here, e.g. `m => self.dispatch_user(m, ctx)`.
            _ => Err(LogicError::Event(format!(
                "unknown method '{}' on TestPlan",
                method
            ))),
        }
    }
}
