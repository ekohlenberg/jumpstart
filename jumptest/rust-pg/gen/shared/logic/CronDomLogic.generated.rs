// <auto-generated> Rust port of templateLogic.generated.cs for `CronDom`.
// Regenerated on every run. The CRUD operations are normal `pub(crate)` methods;
// `dispatch` is the generated string->method router; `exec` is the authorized
// entry point (runs the proxy hooks via LogicExec). Hand-written operations go
// in CronDomLogic.user.rs (the `dispatch_user` hook). </auto-generated>

use common::*;
use domain::*;
use persist::*;
use serde_json::Value;

use crate::base_logic::LogicError;
use crate::logic_context::LogicContext;
use crate::logic_exec::{ExecProof, LogicDispatch, LogicExec};

pub struct CronDomLogic;

impl CronDomLogic {
    pub fn new() -> Self {
        CronDomLogic
    }

    /// Authorized entry point: logging + authorization (+ events) then dispatch.
    pub fn exec(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call(&CronDomLogic::new(), method, ctx)
    }

    /// Bypass the hooks/authorization. Use only where no principal exists
    /// (import/export, tests).
    pub fn exec_unchecked(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call_unchecked(&CronDomLogic::new(), method, ctx)
    }

    // ── raw operations (reachable only via dispatch / LogicExec) ─────────

    pub(crate) fn select(&self) -> Result<Vec<CronDom>, LogicError> {
        Logger::debug("Processing CronDomLogic select");
        Ok(DBPersist::execute_query_into::<CronDom>(
            "core.cron_dom-select",
            "default",
        )?)
    }

    pub(crate) fn get(&self, id: i64) -> Result<Option<CronDom>, LogicError> {
        Logger::debug(format!("Processing CronDomLogic get ID={}", id));
        let sql = format!(
            "SELECT * FROM core.cron_dom WHERE id = {} AND is_active = 1",
            id
        );
        Ok(DBPersist::select_into::<CronDom>(&sql, "default")?
            .into_iter()
            .next())
    }

    pub(crate) fn view(&self, id: i64) -> Result<Option<CronDomView>, LogicError> {
        Logger::debug(format!("Processing CronDomLogic view ID={}", id));
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        Ok(DBPersist::execute_query_into_filtered::<CronDomView>(
            "core.cron_dom-get",
            &filter,
            "default",
        )?
        .into_iter()
        .next())
    }

    pub(crate) fn history(&self, id: i64) -> Result<Vec<CronDomHistory>, LogicError> {
        let sql = format!(
            "SELECT * FROM core.cron_dom WHERE id = {} ORDER BY txn_id DESC",
            id
        );
        Ok(DBPersist::select_into::<CronDomHistory>(&sql, "default")?)
    }

    pub(crate) fn children(
        &self,
        id: i64,
        child_suffix: &str,
    ) -> Result<Vec<BaseObject>, LogicError> {
        let query_name = format!("core.cron_dom-children-{}", child_suffix);
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        let rows = DBPersist::execute_query_rows_filtered(&query_name, &filter, "default")?;
        Ok(rows
            .into_iter()
            .map(|row| {
                let mut o = BaseObject::new();
                for (k, v) in row {
                    o.set(&k, v);
                }
                o
            })
            .collect())
    }

    pub(crate) fn insert(&self, crondom: &mut CronDom) -> Result<(), LogicError> {
        Logger::debug("Processing CronDomLogic insert");
        crondom.base.set("is_active", Value::from(1));
        DBPersist::insert(crondom, "default")?;
        Ok(())
    }

    pub(crate) fn update(&self, id: i64, crondom: &mut CronDom) -> Result<(), LogicError> {
        Logger::debug(format!("Processing CronDomLogic update ID={}", id));
        crondom.base.set("id", Value::from(id));
        // A normal save always (re)activates the record -- explicit here
        // (matching insert/put above) now that db_persist_audit::update() no
        // longer forces is_active=1 itself, since delete() below relies on
        // that function honoring the is_active it sets.
        crondom.base.set("is_active", Value::from(1));
        DBPersist::update(crondom, "default")?;
        Ok(())
    }

    pub(crate) fn put(&self, crondom: &mut CronDom) -> Result<(), LogicError> {
        Logger::debug("Processing CronDomLogic put");
        crondom.base.set("is_active", Value::from(1));
        DBPersist::put(crondom, "default")?;
        Ok(())
    }

    pub(crate) fn delete(&self, id: i64) -> Result<(), LogicError> {
        if let Some(mut crondom) = self.get(id)? {
            crondom.base.set("is_active", Value::from(0));
            DBPersist::update(&mut crondom, "default")?;
        }
        Ok(())
    }
}

impl LogicDispatch for CronDomLogic {
    const DOMAIN: &'static str = "CronDom";

    fn dispatch(
        &self,
        method: &str,
        ctx: &mut LogicContext,
        _proof: &ExecProof,
    ) -> Result<Value, LogicError> {
        match method {
            "select" => {
                let rows = self.select()?;
                let cols = CronDom::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.base.data, &cols)).collect(),
                ))
            }
            "get" => {
                let found = self.get(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.base.data, &CronDom::columns()))
                    .unwrap_or(Value::Null))
            }
            "view" => {
                let found = self.view(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.inner.base.data, &CronDom::columns()))
                    .unwrap_or(Value::Null))
            }
            "history" => {
                let rows = self.history(ctx.id())?;
                let cols = CronDom::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.inner.base.data, &cols)).collect(),
                ))
            }
            "children" => {
                let rows = self.children(ctx.id(), &ctx.arg_str("child_suffix"))?;
                Ok(serde_json::to_value(rows.iter().map(|o| &o.data).collect::<Vec<_>>())
                    .unwrap_or(Value::Null))
            }
            "insert" => {
                let mut o = CronDom::from_base(ctx.transaction.clone());
                self.insert(&mut o)?;
                let v = to_typed_json(&o.base.data, &CronDom::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "update" => {
                let mut o = CronDom::from_base(ctx.transaction.clone());
                self.update(ctx.id(), &mut o)?;
                let v = to_typed_json(&o.base.data, &CronDom::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "put" => {
                let mut o = CronDom::from_base(ctx.transaction.clone());
                self.put(&mut o)?;
                let v = to_typed_json(&o.base.data, &CronDom::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "delete" => {
                self.delete(ctx.id())?;
                Ok(Value::Null)
            }
            "enum" => {
                // id + display-string pairs for dropdowns (matches the .NET
                // /api/<obj>/enum endpoint).
                let rows = self.select()?;
                let enums: Vec<EnumHelper> = rows
                    .iter()
                    .map(|o| EnumHelper::new(o.id(), o.base.get_rwk_string(false)))
                    .collect();
                Ok(serde_json::to_value(&enums).unwrap_or(Value::Null))
            }
            // Custom operations: add an arm in the user file's `dispatch_user`
            // and delegate here, e.g. `m => self.dispatch_user(m, ctx)`.
            _ => Err(LogicError::Event(format!(
                "unknown method '{}' on CronDom",
                method
            ))),
        }
    }
}
