// <auto-generated> Rust port of templateLogic.generated.cs for `TestResult`.
// Regenerated on every run. The CRUD operations are normal `pub(crate)` methods;
// `dispatch` is the generated string->method router; `exec` is the authorized
// entry point (runs the proxy hooks via LogicExec). Hand-written operations go
// in TestResultLogic.user.rs (the `dispatch_user` hook). </auto-generated>

use common::*;
use domain::*;
use persist::*;
use serde_json::Value;

use crate::base_logic::LogicError;
use crate::logic_context::LogicContext;
use crate::logic_exec::{ExecProof, LogicDispatch, LogicExec};

pub struct TestResultLogic;

impl TestResultLogic {
    pub fn new() -> Self {
        TestResultLogic
    }

    /// Authorized entry point: logging + authorization (+ events) then dispatch.
    pub fn exec(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call(&TestResultLogic::new(), method, ctx)
    }

    /// Bypass the hooks/authorization. Use only where no principal exists
    /// (import/export, tests).
    pub fn exec_unchecked(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call_unchecked(&TestResultLogic::new(), method, ctx)
    }

    // ── raw operations (reachable only via dispatch / LogicExec) ─────────

    pub(crate) fn select(&self) -> Result<Vec<TestResult>, LogicError> {
        Logger::debug("Processing TestResultLogic select");
        Ok(DBPersist::execute_query_into::<TestResult>(
            "app.test_result-select",
            "default",
        )?)
    }

    pub(crate) fn get(&self, id: i64) -> Result<Option<TestResult>, LogicError> {
        Logger::debug(format!("Processing TestResultLogic get ID={}", id));
        let sql = format!(
            "SELECT * FROM app.test_result WHERE id = {} AND is_active = 1",
            id
        );
        Ok(DBPersist::select_into::<TestResult>(&sql, "default")?
            .into_iter()
            .next())
    }

    pub(crate) fn view(&self, id: i64) -> Result<Option<TestResultView>, LogicError> {
        Logger::debug(format!("Processing TestResultLogic view ID={}", id));
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        Ok(DBPersist::execute_query_into_filtered::<TestResultView>(
            "app.test_result-get",
            &filter,
            "default",
        )?
        .into_iter()
        .next())
    }

    pub(crate) fn history(&self, id: i64) -> Result<Vec<TestResultHistory>, LogicError> {
        let sql = format!(
            "SELECT * FROM app.test_result WHERE id = {} ORDER BY txn_id DESC",
            id
        );
        Ok(DBPersist::select_into::<TestResultHistory>(&sql, "default")?)
    }

    pub(crate) fn children(
        &self,
        id: i64,
        child_suffix: &str,
    ) -> Result<Vec<BaseObject>, LogicError> {
        let query_name = format!("app.test_result-children-{}", child_suffix);
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        let rows = DBPersist::execute_query_rows_filtered(&query_name, &filter, "default")?;
        Ok(rows
            .into_iter()
            .map(|row| {
                let mut o = BaseObject::new();
                for (k, v) in row {
                    o.set(&k, v);
                }
                o
            })
            .collect())
    }

    pub(crate) fn insert(&self, testresult: &mut TestResult) -> Result<(), LogicError> {
        Logger::debug("Processing TestResultLogic insert");
        testresult.base.set("is_active", Value::from(1));
        DBPersist::insert(testresult, "default")?;
        Ok(())
    }

    pub(crate) fn update(&self, id: i64, testresult: &mut TestResult) -> Result<(), LogicError> {
        Logger::debug(format!("Processing TestResultLogic update ID={}", id));
        testresult.base.set("id", Value::from(id));
        DBPersist::update(testresult, "default")?;
        Ok(())
    }

    pub(crate) fn put(&self, testresult: &mut TestResult) -> Result<(), LogicError> {
        Logger::debug("Processing TestResultLogic put");
        testresult.base.set("is_active", Value::from(1));
        DBPersist::put(testresult, "default")?;
        Ok(())
    }

    pub(crate) fn delete(&self, id: i64) -> Result<(), LogicError> {
        if let Some(mut testresult) = self.get(id)? {
            testresult.base.set("is_active", Value::from(0));
            DBPersist::update(&mut testresult, "default")?;
        }
        Ok(())
    }
}

impl LogicDispatch for TestResultLogic {
    const DOMAIN: &'static str = "TestResult";

    fn dispatch(
        &self,
        method: &str,
        ctx: &mut LogicContext,
        _proof: &ExecProof,
    ) -> Result<Value, LogicError> {
        match method {
            "select" => {
                let rows = self.select()?;
                let cols = TestResult::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.base.data, &cols)).collect(),
                ))
            }
            "get" => {
                let found = self.get(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.base.data, &TestResult::columns()))
                    .unwrap_or(Value::Null))
            }
            "view" => {
                let found = self.view(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.inner.base.data, &TestResult::columns()))
                    .unwrap_or(Value::Null))
            }
            "history" => {
                let rows = self.history(ctx.id())?;
                let cols = TestResult::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.inner.base.data, &cols)).collect(),
                ))
            }
            "children" => {
                let rows = self.children(ctx.id(), &ctx.arg_str("child_suffix"))?;
                Ok(serde_json::to_value(rows.iter().map(|o| &o.data).collect::<Vec<_>>())
                    .unwrap_or(Value::Null))
            }
            "insert" => {
                let mut o = TestResult::from_base(ctx.transaction.clone());
                self.insert(&mut o)?;
                let v = to_typed_json(&o.base.data, &TestResult::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "update" => {
                let mut o = TestResult::from_base(ctx.transaction.clone());
                self.update(ctx.id(), &mut o)?;
                let v = to_typed_json(&o.base.data, &TestResult::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "put" => {
                let mut o = TestResult::from_base(ctx.transaction.clone());
                self.put(&mut o)?;
                let v = to_typed_json(&o.base.data, &TestResult::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "delete" => {
                self.delete(ctx.id())?;
                Ok(Value::Null)
            }
            "enum" => {
                // id + display-string pairs for dropdowns (matches the .NET
                // /api/<obj>/enum endpoint).
                let rows = self.select()?;
                let enums: Vec<EnumHelper> = rows
                    .iter()
                    .map(|o| EnumHelper::new(o.id(), o.base.get_rwk_string(false)))
                    .collect();
                Ok(serde_json::to_value(&enums).unwrap_or(Value::Null))
            }
            // Custom operations: add an arm in the user file's `dispatch_user`
            // and delegate here, e.g. `m => self.dispatch_user(m, ctx)`.
            _ => Err(LogicError::Event(format!(
                "unknown method '{}' on TestResult",
                method
            ))),
        }
    }
}
