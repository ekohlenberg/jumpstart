// <auto-generated> Rust port of templateLogic.generated.cs for `ExecLog`.
// Regenerated on every run. The CRUD operations are normal `pub(crate)` methods;
// `dispatch` is the generated string->method router; `exec` is the authorized
// entry point (runs the proxy hooks via LogicExec). Hand-written operations go
// in ExecLogLogic.user.rs (the `dispatch_user` hook). </auto-generated>

use common::*;
use domain::*;
use persist::*;
use serde_json::Value;

use crate::base_logic::LogicError;
use crate::logic_context::LogicContext;
use crate::logic_exec::{ExecProof, LogicDispatch, LogicExec};

pub struct ExecLogLogic;

impl ExecLogLogic {
    pub fn new() -> Self {
        ExecLogLogic
    }

    /// Authorized entry point: logging + authorization (+ events) then dispatch.
    pub fn exec(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call(&ExecLogLogic::new(), method, ctx)
    }

    /// Bypass the hooks/authorization. Use only where no principal exists
    /// (import/export, tests).
    pub fn exec_unchecked(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call_unchecked(&ExecLogLogic::new(), method, ctx)
    }

    // ── raw operations (reachable only via dispatch / LogicExec) ─────────

    pub(crate) fn select(&self) -> Result<Vec<ExecLog>, LogicError> {
        Logger::debug("Processing ExecLogLogic select");
        Ok(DBPersist::execute_query_into::<ExecLog>(
            "core.exec_log-select",
            "default",
        )?)
    }

    pub(crate) fn get(&self, id: i64) -> Result<Option<ExecLog>, LogicError> {
        Logger::debug(format!("Processing ExecLogLogic get ID={}", id));
        let sql = format!(
            "SELECT * FROM core.exec_log WHERE id = {} AND is_active = 1",
            id
        );
        Ok(DBPersist::select_into::<ExecLog>(&sql, "default")?
            .into_iter()
            .next())
    }

    pub(crate) fn view(&self, id: i64) -> Result<Option<ExecLogView>, LogicError> {
        Logger::debug(format!("Processing ExecLogLogic view ID={}", id));
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        Ok(DBPersist::execute_query_into_filtered::<ExecLogView>(
            "core.exec_log-get",
            &filter,
            "default",
        )?
        .into_iter()
        .next())
    }

    pub(crate) fn history(&self, id: i64) -> Result<Vec<ExecLogHistory>, LogicError> {
        let sql = format!(
            "SELECT * FROM core.exec_log WHERE id = {} ORDER BY txn_id DESC",
            id
        );
        Ok(DBPersist::select_into::<ExecLogHistory>(&sql, "default")?)
    }

    pub(crate) fn children(
        &self,
        id: i64,
        child_suffix: &str,
    ) -> Result<Vec<BaseObject>, LogicError> {
        let query_name = format!("core.exec_log-children-{}", child_suffix);
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        let rows = DBPersist::execute_query_rows_filtered(&query_name, &filter, "default")?;
        Ok(rows
            .into_iter()
            .map(|row| {
                let mut o = BaseObject::new();
                for (k, v) in row {
                    o.set(&k, v);
                }
                o
            })
            .collect())
    }

    pub(crate) fn insert(&self, execlog: &mut ExecLog) -> Result<(), LogicError> {
        Logger::debug("Processing ExecLogLogic insert");
        execlog.base.set("is_active", Value::from(1));
        DBPersist::insert(execlog, "default")?;
        Ok(())
    }

    pub(crate) fn update(&self, id: i64, execlog: &mut ExecLog) -> Result<(), LogicError> {
        Logger::debug(format!("Processing ExecLogLogic update ID={}", id));
        execlog.base.set("id", Value::from(id));
        DBPersist::update(execlog, "default")?;
        Ok(())
    }

    pub(crate) fn put(&self, execlog: &mut ExecLog) -> Result<(), LogicError> {
        Logger::debug("Processing ExecLogLogic put");
        execlog.base.set("is_active", Value::from(1));
        DBPersist::put(execlog, "default")?;
        Ok(())
    }

    pub(crate) fn delete(&self, id: i64) -> Result<(), LogicError> {
        if let Some(mut execlog) = self.get(id)? {
            execlog.base.set("is_active", Value::from(0));
            DBPersist::update(&mut execlog, "default")?;
        }
        Ok(())
    }
}

impl LogicDispatch for ExecLogLogic {
    const DOMAIN: &'static str = "ExecLog";

    fn dispatch(
        &self,
        method: &str,
        ctx: &mut LogicContext,
        _proof: &ExecProof,
    ) -> Result<Value, LogicError> {
        match method {
            "select" => {
                let rows = self.select()?;
                let cols = ExecLog::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.base.data, &cols)).collect(),
                ))
            }
            "get" => {
                let found = self.get(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.base.data, &ExecLog::columns()))
                    .unwrap_or(Value::Null))
            }
            "view" => {
                let found = self.view(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.inner.base.data, &ExecLog::columns()))
                    .unwrap_or(Value::Null))
            }
            "history" => {
                let rows = self.history(ctx.id())?;
                let cols = ExecLog::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.inner.base.data, &cols)).collect(),
                ))
            }
            "children" => {
                let rows = self.children(ctx.id(), &ctx.arg_str("child_suffix"))?;
                Ok(serde_json::to_value(rows.iter().map(|o| &o.data).collect::<Vec<_>>())
                    .unwrap_or(Value::Null))
            }
            "insert" => {
                let mut o = ExecLog::from_base(ctx.transaction.clone());
                self.insert(&mut o)?;
                let v = to_typed_json(&o.base.data, &ExecLog::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "update" => {
                let mut o = ExecLog::from_base(ctx.transaction.clone());
                self.update(ctx.id(), &mut o)?;
                let v = to_typed_json(&o.base.data, &ExecLog::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "put" => {
                let mut o = ExecLog::from_base(ctx.transaction.clone());
                self.put(&mut o)?;
                let v = to_typed_json(&o.base.data, &ExecLog::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "delete" => {
                self.delete(ctx.id())?;
                Ok(Value::Null)
            }
            "enum" => {
                // id + display-string pairs for dropdowns (matches the .NET
                // /api/<obj>/enum endpoint).
                let rows = self.select()?;
                let enums: Vec<EnumHelper> = rows
                    .iter()
                    .map(|o| EnumHelper::new(o.id(), o.base.get_rwk_string(false)))
                    .collect();
                Ok(serde_json::to_value(&enums).unwrap_or(Value::Null))
            }
            // Custom operations: add an arm in the user file's `dispatch_user`
            // and delegate here, e.g. `m => self.dispatch_user(m, ctx)`.
            _ => Err(LogicError::Event(format!(
                "unknown method '{}' on ExecLog",
                method
            ))),
        }
    }
}
