// <auto-generated> Rust port of templateLogic.generated.cs for `Sql`.
// Regenerated on every run. The CRUD operations are normal `pub(crate)` methods;
// `dispatch` is the generated string->method router; `exec` is the authorized
// entry point (runs the proxy hooks via LogicExec). Hand-written operations go
// in SqlLogic.user.rs (the `dispatch_user` hook). </auto-generated>

use common::*;
use domain::*;
use persist::*;
use serde_json::Value;

use crate::base_logic::LogicError;
use crate::logic_context::LogicContext;
use crate::logic_exec::{ExecProof, LogicDispatch, LogicExec};

pub struct SqlLogic;

impl SqlLogic {
    pub fn new() -> Self {
        SqlLogic
    }

    /// Authorized entry point: logging + authorization (+ events) then dispatch.
    pub fn exec(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call(&SqlLogic::new(), method, ctx)
    }

    /// Bypass the hooks/authorization. Use only where no principal exists
    /// (import/export, tests).
    pub fn exec_unchecked(method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
        LogicExec::call_unchecked(&SqlLogic::new(), method, ctx)
    }

    // ── raw operations (reachable only via dispatch / LogicExec) ─────────

    pub(crate) fn select(&self) -> Result<Vec<Sql>, LogicError> {
        Logger::debug("Processing SqlLogic select");
        Ok(DBPersist::execute_query_into::<Sql>(
            "core.sql-select",
            "default",
        )?)
    }

    pub(crate) fn get(&self, id: i64) -> Result<Option<Sql>, LogicError> {
        Logger::debug(format!("Processing SqlLogic get ID={}", id));
        let sql = format!(
            "SELECT * FROM core.sql WHERE id = {} AND is_active = 1",
            id
        );
        Ok(DBPersist::select_into::<Sql>(&sql, "default")?
            .into_iter()
            .next())
    }

    pub(crate) fn view(&self, id: i64) -> Result<Option<SqlView>, LogicError> {
        Logger::debug(format!("Processing SqlLogic view ID={}", id));
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        Ok(DBPersist::execute_query_into_filtered::<SqlView>(
            "core.sql-get",
            &filter,
            "default",
        )?
        .into_iter()
        .next())
    }

    pub(crate) fn history(&self, id: i64) -> Result<Vec<SqlHistory>, LogicError> {
        let sql = format!(
            "SELECT * FROM core.sql WHERE id = {} ORDER BY txn_id DESC",
            id
        );
        Ok(DBPersist::select_into::<SqlHistory>(&sql, "default")?)
    }

    pub(crate) fn children(
        &self,
        id: i64,
        child_suffix: &str,
    ) -> Result<Vec<BaseObject>, LogicError> {
        let query_name = format!("core.sql-children-{}", child_suffix);
        let mut filter = BaseObject::new();
        filter.set("id", Value::from(id));
        let rows = DBPersist::execute_query_rows_filtered(&query_name, &filter, "default")?;
        Ok(rows
            .into_iter()
            .map(|row| {
                let mut o = BaseObject::new();
                for (k, v) in row {
                    o.set(&k, v);
                }
                o
            })
            .collect())
    }

    pub(crate) fn insert(&self, sql: &mut Sql) -> Result<(), LogicError> {
        Logger::debug("Processing SqlLogic insert");
        sql.base.set("is_active", Value::from(1));
        DBPersist::insert(sql, "default")?;
        Ok(())
    }

    pub(crate) fn update(&self, id: i64, sql: &mut Sql) -> Result<(), LogicError> {
        Logger::debug(format!("Processing SqlLogic update ID={}", id));
        sql.base.set("id", Value::from(id));
        DBPersist::update(sql, "default")?;
        Ok(())
    }

    pub(crate) fn put(&self, sql: &mut Sql) -> Result<(), LogicError> {
        Logger::debug("Processing SqlLogic put");
        sql.base.set("is_active", Value::from(1));
        DBPersist::put(sql, "default")?;
        Ok(())
    }

    pub(crate) fn delete(&self, id: i64) -> Result<(), LogicError> {
        if let Some(mut sql) = self.get(id)? {
            sql.base.set("is_active", Value::from(0));
            DBPersist::update(&mut sql, "default")?;
        }
        Ok(())
    }
}

impl LogicDispatch for SqlLogic {
    const DOMAIN: &'static str = "Sql";

    fn dispatch(
        &self,
        method: &str,
        ctx: &mut LogicContext,
        _proof: &ExecProof,
    ) -> Result<Value, LogicError> {
        match method {
            "select" => {
                let rows = self.select()?;
                let cols = Sql::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.base.data, &cols)).collect(),
                ))
            }
            "get" => {
                let found = self.get(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.base.data, &Sql::columns()))
                    .unwrap_or(Value::Null))
            }
            "view" => {
                let found = self.view(ctx.id())?;
                Ok(found
                    .map(|o| to_typed_json(&o.inner.base.data, &Sql::columns()))
                    .unwrap_or(Value::Null))
            }
            "history" => {
                let rows = self.history(ctx.id())?;
                let cols = Sql::columns();
                Ok(Value::Array(
                    rows.iter().map(|o| to_typed_json(&o.inner.base.data, &cols)).collect(),
                ))
            }
            "children" => {
                let rows = self.children(ctx.id(), &ctx.arg_str("child_suffix"))?;
                Ok(serde_json::to_value(rows.iter().map(|o| &o.data).collect::<Vec<_>>())
                    .unwrap_or(Value::Null))
            }
            "insert" => {
                let mut o = Sql::from_base(ctx.transaction.clone());
                self.insert(&mut o)?;
                let v = to_typed_json(&o.base.data, &Sql::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "update" => {
                let mut o = Sql::from_base(ctx.transaction.clone());
                self.update(ctx.id(), &mut o)?;
                let v = to_typed_json(&o.base.data, &Sql::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "put" => {
                let mut o = Sql::from_base(ctx.transaction.clone());
                self.put(&mut o)?;
                let v = to_typed_json(&o.base.data, &Sql::columns());
                ctx.transaction = o.base;
                Ok(v)
            }
            "delete" => {
                self.delete(ctx.id())?;
                Ok(Value::Null)
            }
            "enum" => {
                // id + display-string pairs for dropdowns (matches the .NET
                // /api/<obj>/enum endpoint).
                let rows = self.select()?;
                let enums: Vec<EnumHelper> = rows
                    .iter()
                    .map(|o| EnumHelper::new(o.id(), o.base.get_rwk_string(false)))
                    .collect();
                Ok(serde_json::to_value(&enums).unwrap_or(Value::Null))
            }
            // Custom operations: add an arm in the user file's `dispatch_user`
            // and delegate here, e.g. `m => self.dispatch_user(m, ctx)`.
            _ => Err(LogicError::Event(format!(
                "unknown method '{}' on Sql",
                method
            ))),
        }
    }
}
