@page "/authentication/{action}"
@*
   Authentication is DISABLED until the app stabilizes. The OIDC callback view is
   inert. To re-enable, add the Microsoft.AspNetCore.Components.WebAssembly.Authentication
   using and render a RemoteAuthenticatorView bound to the Action parameter
   (see the .NET template / git history).
*@
<p>Authentication is currently disabled.</p>

@code {
    [Parameter]
    public string? Action { get; set; }
}
