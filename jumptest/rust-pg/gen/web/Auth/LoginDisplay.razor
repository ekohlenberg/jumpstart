@*
   Authentication is DISABLED until the app stabilizes. This component (the
   Auth0 login / logout button) is intentionally inert. To re-enable, restore the
   <AuthorizeView> markup with the Log in / Log out buttons and the BeginLogout
   handler, then reference <LoginDisplay /> from MainLayout again.
*@
