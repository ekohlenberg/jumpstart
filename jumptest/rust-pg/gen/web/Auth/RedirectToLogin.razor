@*
   Authentication is DISABLED until the app stabilizes. This component (which
   redirected unauthenticated users to the Auth0 login) is intentionally inert.
   To re-enable, restore the OnInitialized redirect to "authentication/login"
   and reference <RedirectToLogin /> from App.razor's <NotAuthorized> again.
*@
