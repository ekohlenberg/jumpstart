{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "jumptest"
  },
  "_note": "The live database connection is read from ~/.jumptest.json by the common Config loader. The Scheduler binds the first free port in 5000-5100, self-registers in core.server_node as a Scheduler node, and fires cron-scheduled workflows, dispatching them to script agents."
}
