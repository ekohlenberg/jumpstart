// <auto-generated> Rust port of template.test.generated.cs for `PrincipalOrg`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (PrincipalOrgLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct PrincipalOrgTest;

impl PrincipalOrgTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        record.set("org_id", serde_json::json!(BaseTest::get_last_id("org")));
            
        record.set("principal_id", serde_json::json!(BaseTest::get_last_id("principal")));
                    Logger::info(format!("Testing PrincipalOrgLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        PrincipalOrgLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("principalorg", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = PrincipalOrgLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"org_id", "principal_id",         ];
        BaseTest::assert_persisted("PrincipalOrg::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("principalorg");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = PrincipalOrgLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        record.set("org_id", serde_json::json!(BaseTest::get_last_id("org")));
            
        record.set("principal_id", serde_json::json!(BaseTest::get_last_id("principal")));
                    Logger::info(format!("Testing PrincipalOrgLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        PrincipalOrgLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = PrincipalOrgLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"org_id", "principal_id",         ];
        BaseTest::assert_persisted("PrincipalOrg::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
