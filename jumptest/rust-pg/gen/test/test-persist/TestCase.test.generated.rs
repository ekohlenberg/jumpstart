// <auto-generated> Rust port of template.test.generated.cs for `TestCase`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (TestCaseLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct TestCaseTest;

impl TestCaseTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        record.set("test_plan_id", serde_json::json!(BaseTest::get_last_id("testplan")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "50");
        record.set("code", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "100");
        record.set("area", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("title", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("preconditions", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("steps", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("expected_result", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "10");
        record.set("priority", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "100");
        record.set("component", v);
                Logger::info(format!("Testing TestCaseLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        TestCaseLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("testcase", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = TestCaseLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"test_plan_id", "code", "area", "title", "preconditions", "steps", "expected_result", "priority", "component",         ];
        BaseTest::assert_persisted("TestCase::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("testcase");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = TestCaseLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        record.set("test_plan_id", serde_json::json!(BaseTest::get_last_id("testplan")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "50");
        record.set("code", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "100");
        record.set("area", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("title", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("preconditions", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("steps", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("expected_result", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "10");
        record.set("priority", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "100");
        record.set("component", v);
                Logger::info(format!("Testing TestCaseLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        TestCaseLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = TestCaseLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"test_plan_id", "code", "area", "title", "preconditions", "steps", "expected_result", "priority", "component",         ];
        BaseTest::assert_persisted("TestCase::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
