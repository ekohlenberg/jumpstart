// <auto-generated> Rust port of template.test.generated.cs for `OpRoleMember`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (OpRoleMemberLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct OpRoleMemberTest;

impl OpRoleMemberTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        record.set("principal_id", serde_json::json!(BaseTest::get_last_id("principal")));
            
        record.set("op_role_id", serde_json::json!(BaseTest::get_last_id("oprole")));
                    Logger::info(format!("Testing OpRoleMemberLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        OpRoleMemberLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("oprolemember", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = OpRoleMemberLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"principal_id", "op_role_id",         ];
        BaseTest::assert_persisted("OpRoleMember::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("oprolemember");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = OpRoleMemberLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        record.set("principal_id", serde_json::json!(BaseTest::get_last_id("principal")));
            
        record.set("op_role_id", serde_json::json!(BaseTest::get_last_id("oprole")));
                    Logger::info(format!("Testing OpRoleMemberLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        OpRoleMemberLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = OpRoleMemberLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"principal_id", "op_role_id",         ];
        BaseTest::assert_persisted("OpRoleMember::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
