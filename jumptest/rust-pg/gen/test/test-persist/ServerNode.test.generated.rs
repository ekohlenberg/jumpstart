// <auto-generated> Rust port of template.test.generated.cs for `ServerNode`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (ServerNodeLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct ServerNodeTest;

impl ServerNodeTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        record.set("server_node_type_id", serde_json::json!(BaseTest::get_last_id("servernodetype")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("hostname", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("ip_address", v);
        
        let v = BaseTest::get_test_data(&record, "INTEGER", "random", "NULL");
        record.set("port", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("username", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("url", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("user_domain", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("os_name", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("os_version", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("architecture", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("registered_at", v);
        
        record.set("server_node_status_id", serde_json::json!(BaseTest::get_last_id("servernodestatus")));
                    Logger::info(format!("Testing ServerNodeLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ServerNodeLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("servernode", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = ServerNodeLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"server_node_type_id", "hostname", "ip_address", "port", "username", "url", "user_domain", "os_name", "os_version", "architecture", "server_node_status_id",         ];
        BaseTest::assert_persisted("ServerNode::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("servernode");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = ServerNodeLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        record.set("server_node_type_id", serde_json::json!(BaseTest::get_last_id("servernodetype")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("hostname", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("ip_address", v);
        
        let v = BaseTest::get_test_data(&record, "INTEGER", "random", "NULL");
        record.set("port", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("username", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("url", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("user_domain", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("os_name", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("os_version", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("architecture", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("registered_at", v);
        
        record.set("server_node_status_id", serde_json::json!(BaseTest::get_last_id("servernodestatus")));
                    Logger::info(format!("Testing ServerNodeLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        ServerNodeLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = ServerNodeLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"server_node_type_id", "hostname", "ip_address", "port", "username", "url", "user_domain", "os_name", "os_version", "architecture", "server_node_status_id",         ];
        BaseTest::assert_persisted("ServerNode::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
