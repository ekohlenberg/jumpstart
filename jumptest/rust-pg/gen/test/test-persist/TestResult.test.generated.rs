// <auto-generated> Rust port of template.test.generated.cs for `TestResult`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (TestResultLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct TestResultTest;

impl TestResultTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        record.set("test_run_id", serde_json::json!(BaseTest::get_last_id("testrun")));
            
        record.set("test_case_id", serde_json::json!(BaseTest::get_last_id("testcase")));
            
        record.set("test_result_status_id", serde_json::json!(BaseTest::get_last_id("testresultstatus")));
            
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("executed_at", v);
        
        record.set("executed_by_id", serde_json::json!(BaseTest::get_last_id("principal")));
            
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("actual_result", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("notes", v);
                Logger::info(format!("Testing TestResultLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        TestResultLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("testresult", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = TestResultLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"test_run_id", "test_case_id", "test_result_status_id", "executed_by_id", "actual_result", "notes",         ];
        BaseTest::assert_persisted("TestResult::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("testresult");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = TestResultLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        record.set("test_run_id", serde_json::json!(BaseTest::get_last_id("testrun")));
            
        record.set("test_case_id", serde_json::json!(BaseTest::get_last_id("testcase")));
            
        record.set("test_result_status_id", serde_json::json!(BaseTest::get_last_id("testresultstatus")));
            
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("executed_at", v);
        
        record.set("executed_by_id", serde_json::json!(BaseTest::get_last_id("principal")));
            
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("actual_result", v);
        
        let v = BaseTest::get_test_data(&record, "TEXT", "random", "NULL");
        record.set("notes", v);
                Logger::info(format!("Testing TestResultLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        TestResultLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = TestResultLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"test_run_id", "test_case_id", "test_result_status_id", "executed_by_id", "actual_result", "notes",         ];
        BaseTest::assert_persisted("TestResult::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
