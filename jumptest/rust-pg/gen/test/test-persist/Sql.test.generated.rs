// <auto-generated> Rust port of template.test.generated.cs for `Sql`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (SqlLogic::exec) with a LogicContext. </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct SqlTest;

impl SqlTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("name", v);
        
        record.set("data_source_id", serde_json::json!(BaseTest::get_last_id("datasource")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "4096");
        record.set("sql_text", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "1000");
        record.set("description", v);
                Logger::info(format!("Testing SqlLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        SqlLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("sql", id);
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("sql");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = SqlLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("name", v);
        
        record.set("data_source_id", serde_json::json!(BaseTest::get_last_id("datasource")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "4096");
        record.set("sql_text", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "1000");
        record.set("description", v);
                Logger::info(format!("Testing SqlLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        SqlLogic::exec("update", &mut ctx)?;
        Ok(())
    }
}
