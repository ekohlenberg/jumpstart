// <auto-generated> Rust port of template.test.generated.cs for `Schedule`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (ScheduleLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct ScheduleTest;

impl ScheduleTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("name", v);
        
        record.set("cron_every_id", serde_json::json!(BaseTest::get_last_id("cronevery")));
            
        record.set("cron_minute_id", serde_json::json!(BaseTest::get_last_id("cronminute")));
            
        record.set("cron_hour_id", serde_json::json!(BaseTest::get_last_id("cronhour")));
            
        record.set("cron_dom_id", serde_json::json!(BaseTest::get_last_id("crondom")));
            
        record.set("cron_month_id", serde_json::json!(BaseTest::get_last_id("cronmonth")));
            
        record.set("cron_dow_id", serde_json::json!(BaseTest::get_last_id("crondow")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("schedule_label", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("next_run_time", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("last_run_time", v);
                Logger::info(format!("Testing ScheduleLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ScheduleLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("schedule", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = ScheduleLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"name", "cron_every_id", "cron_minute_id", "cron_hour_id", "cron_dom_id", "cron_month_id", "cron_dow_id", "schedule_label",         ];
        BaseTest::assert_persisted("Schedule::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("schedule");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = ScheduleLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("name", v);
        
        record.set("cron_every_id", serde_json::json!(BaseTest::get_last_id("cronevery")));
            
        record.set("cron_minute_id", serde_json::json!(BaseTest::get_last_id("cronminute")));
            
        record.set("cron_hour_id", serde_json::json!(BaseTest::get_last_id("cronhour")));
            
        record.set("cron_dom_id", serde_json::json!(BaseTest::get_last_id("crondom")));
            
        record.set("cron_month_id", serde_json::json!(BaseTest::get_last_id("cronmonth")));
            
        record.set("cron_dow_id", serde_json::json!(BaseTest::get_last_id("crondow")));
            
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("schedule_label", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("next_run_time", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("last_run_time", v);
                Logger::info(format!("Testing ScheduleLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        ScheduleLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = ScheduleLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"name", "cron_every_id", "cron_minute_id", "cron_hour_id", "cron_dom_id", "cron_month_id", "cron_dow_id", "schedule_label",         ];
        BaseTest::assert_persisted("Schedule::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
