// <auto-generated> Rust port of template.test.generated.cs for `ExecLog`.
// Regenerated on every run. Drives persistence through the dispatch entry point
// (ExecLogLogic::exec) with a LogicContext. After insert/update, re-selects
// the row via `get` and confirms the persisted values match what was written.
// </auto-generated>

use common::*;
use domain::*;
use logic::*;

use crate::base_test::BaseTest;

pub struct ExecLogTest;

impl ExecLogTest {
    pub fn test_insert() -> Result<(), LogicError> {
        let mut record = BaseObject::new();


        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("token", v);
        
        record.set("workflow_id", serde_json::json!(BaseTest::get_last_id("workflow")));
            
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("start_time", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("end_time", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "4096");
        record.set("stdout", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "4096");
        record.set("stderr", v);
                Logger::info(format!("Testing ExecLogLogic insert: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ExecLogLogic::exec("insert", &mut ctx)?;
        let id = ctx.transaction.get("id").as_i64().unwrap_or_default();
        BaseTest::add_last_id("execlog", id);

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(id);
        let persisted = ExecLogLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"token", "workflow_id", "stdout", "stderr",         ];
        BaseTest::assert_persisted("ExecLog::insert", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }

    pub fn test_update() -> Result<(), LogicError> {
        let last_id = BaseTest::get_last_id("execlog");

        // Exercise the get path.
        let mut get_ctx = LogicContext::for_id(last_id);
        let _existing = ExecLogLogic::exec("get", &mut get_ctx)?;

        let mut record = BaseObject::new();
        record.set("id", serde_json::json!(last_id));


        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "255");
        record.set("token", v);
        
        record.set("workflow_id", serde_json::json!(BaseTest::get_last_id("workflow")));
            
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("start_time", v);
        
        let v = BaseTest::get_test_data(&record, "TIMESTAMP", "random", "NULL");
        record.set("end_time", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "4096");
        record.set("stdout", v);
        
        let v = BaseTest::get_test_data(&record, "VARCHAR", "random", "4096");
        record.set("stderr", v);
                Logger::info(format!("Testing ExecLogLogic update: {}", record.to_json()));
        let mut ctx = LogicContext::for_record(record);
        ctx.set_arg("id", serde_json::json!(last_id));
        ExecLogLogic::exec("update", &mut ctx)?;

        // Re-select the row and confirm the database has what was just written.
        let mut verify_ctx = LogicContext::for_id(last_id);
        let persisted = ExecLogLogic::exec("get", &mut verify_ctx)?;
        let verify_fields: &[&str] = &[
"token", "workflow_id", "stdout", "stderr",         ];
        BaseTest::assert_persisted("ExecLog::update", &ctx.transaction, &persisted, verify_fields)?;
        Ok(())
    }
}
