{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "jumptest"
  },
  "_note": "DB-backed script capabilities read the live connection from ~/.jumptest.json via the common Config loader."
}
