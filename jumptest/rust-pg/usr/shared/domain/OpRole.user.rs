// User extensions for `OpRole`.
//
// This is the Rust equivalent of the .NET `partial class` user file: a second
// `impl` block on the generated type. It is created once (FORCE=FALSE) and is
// never overwritten, so it is safe to hand-edit. It is compiled into the same
// module as OpRole.generated.rs, so the type is already in scope.

impl OpRole {
    // Add custom business logic, computed accessors, or overrides here.
}
