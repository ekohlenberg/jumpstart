{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "@Model.Name"
  },
  "_note": "The live database connection AND all Auth0 M2M settings (inbound validation on this 'scheduler' audience, plus the M2M client used to call ScriptAgents -- see common::auth0 and docs/auth0-m2m.md) are read at runtime from ~/.@(Model.Name).json by the common Config loader -- add an 'auth0' section there (domain, audiences.scheduler, m2m_clients.scriptagent), not here. The Scheduler binds the first free port in 5000-5100, self-registers in core.server_node as a Scheduler node, and fires cron-scheduled workflows, dispatching them to script agents."
}
