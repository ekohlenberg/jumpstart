{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "@Model.Name"
  },
  "_note": "The live database connection is read from ~/.@(Model.Name).json by the common Config loader. The API binds to the first free port in 5200-5300."
}
