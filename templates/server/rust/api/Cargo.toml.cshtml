# <auto-generated> Rust port of api.csproj </auto-generated>
[package]
name = "api"
version = "0.1.0"
edition = "2021"
description = "REST API server for @Model.Name (generated by Jumpstart)"

[[bin]]
name = "api"
path = "main.generated.rs"

[dependencies]
common = { path = "../../shared/common" }
domain = { path = "../../shared/domain" }
logic = { path = "../../shared/logic" }
persist = { path = "../../shared/persist" }
rouille = "3"
serde_json = "1"
# Outbound call to the Scheduler (workflow_run) -- M2M bearer token attachment
# and JWT validation of inbound requests both live in common::auth0.
ureq = "2"
