@page "/"

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.
<!--
<div class="row mt-4">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">Quick Links</h5>
            </div>
            <div class="card-body">
                <ul class="list-group list-group-flush">
                    @foreach( var obj in Model.Objects)
                    {
                    <li class="list-group-item">
                        <a href="@(obj.DomainVar)" class="text-decoration-none">
                            <i class="bi bi-list"></i> @(obj.Label)
                        </a>
                    </li>
                    }
                </ul>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">Application Info</h5>
            </div>
            <div class="card-body">
                <p><strong>Name:</strong> @(Model.Name)</p>
                <p><strong>Namespace:</strong> @(Model.Namespace)</p>
                <p><strong>Objects:</strong> @(Model.Objects.Count)</p>
            </div>
        </div>
    </div>
</div>
-->