@@using Microsoft.AspNetCore.Components.Authorization
@@using Microsoft.AspNetCore.Components.WebAssembly.Authentication

<AuthorizeView>
    <Authorized>
        <span class="me-3 text-secondary small">@@context.User.Identity?.Name</span>
        <button class="btn btn-sm btn-outline-secondary" @@onclick="BeginLogout">Log out</button>
    </Authorized>
    <NotAuthorized>
        <a href="authentication/login" class="btn btn-sm btn-outline-secondary">Log in</a>
    </NotAuthorized>
</AuthorizeView>

@@code {
    [Inject] private NavigationManager Navigation { get; set; } = default!;

    private void BeginLogout()
    {
        Navigation.NavigateTo("authentication/logout");
    }
}
