{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "@Model.Name"
  },
  "_note": "Live DB connection is read from ~/.@(Model.Name).json by the common Config loader. Usage: export --table <domainVar> [--output <path>]."
}
