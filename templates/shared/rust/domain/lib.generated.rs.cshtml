// <auto-generated>
// Crate root for the `domain` library. Regenerated on every run.
//
// Each domain object becomes a module that splices together its generated file
// (gen/shared/domain/<Obj>.generated.rs) and its hand-editable user file
// (usr/shared/domain/<Obj>.user.rs) via include!, mirroring the .NET
// generated + user partial-class pair. The core enum modules are wired in
// separately.
// </auto-generated>
#![allow(dead_code)]
#![allow(clippy::all)]

@foreach (var obj in Model.Objects)
{
    <text>
pub mod @obj.TableName {
    // `use common::*;` is provided by the included generated file.
    include!("@(obj.DomainObj).generated.rs");
    include!("../../../usr/shared/domain/@(obj.DomainObj).user.rs");
}
pub use @(obj.TableName)::*;
    </text>
}

// ---- core enum modules ------------------------------------------------------
// These enums (WorkflowType, ExecStatus, ScriptType, ServerNodeType,
// ServerNodeStatus, OnFailureAction) share names with the lookup-table domain
// objects (e.g. the `workflow_type` table -> `WorkflowType` struct), so they
// are NOT glob-re-exported at the crate root to avoid ambiguity. Reference them
// by module path, e.g. `domain::workflow_core::WorkflowType`.
#[path = "Workflow.core.rs"]
pub mod workflow_core;

#[path = "Execution.core.rs"]
pub mod execution_core;

#[path = "Script.core.rs"]
pub mod script_core;

#[path = "ServerNode.core.rs"]
pub mod server_node_core;
