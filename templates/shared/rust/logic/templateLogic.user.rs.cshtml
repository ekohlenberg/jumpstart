// User logic for `@(Model.DomainObj)`.
//
// Rust equivalent of the .NET `@(Model.DomainObj)UserLogic` partial: a second
// `impl` block on the generated `@(Model.DomainObj)Logic`. Created once
// (FORCE=FALSE) and never overwritten. Compiled into the same module as
// @(Model.DomainObj)Logic.generated.rs, so the type and traits are in scope.

impl @(Model.DomainObj)Logic {
    // Add custom business logic here.
}
