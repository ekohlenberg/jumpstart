// <auto-generated>
// Generated dispatch from an object name to its generated `<Obj>Logic::exec`.
// This is how scripts reach the database "through the Logic layer": `exec` runs
// the proxy hooks (authorization via OpRoleMemberLogic, audit, events) before
// dispatching. Rust has no reflection, so the generator emits the name match.
// Both the PascalCase domain name and the snake_case table name are accepted.
// </auto-generated>

use logic::*;
use serde_json::Value;

/// Run `method` against the named object's logic, through the authorized path.
pub fn exec(domain_obj: &str, method: &str, ctx: &mut LogicContext) -> Result<Value, LogicError> {
    match domain_obj {
@foreach (var obj in Model.Objects)
{
    <text>
        "@(obj.DomainObj)" | "@(obj.TableName)" => @(obj.DomainObj)Logic::exec(method, ctx),
    </text>
}
        _ => Err(LogicError::Event(format!("unknown domain object '{}'", domain_obj))),
    }
}
