{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
	"appsettings": {
    "logwriters": "@(Model.Name).LogFileWriter,@(Model.Name).LogTableWriter,@(Model.Name).LogConsoleWriter",
		"loglevel": "debug" ,
		"db.type": "mssql" 
	}
}

