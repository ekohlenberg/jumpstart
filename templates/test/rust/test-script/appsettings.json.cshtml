{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "@Model.Name"
  },
  "_note": "DB-backed script capabilities read the live connection from ~/.@(Model.Name).json via the common Config loader."
}
