{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "@Model.Name"
  },
  "_note": "Live DB connection is read from ~/.@(Model.Name).json by the common Config loader. A scheduler and at least one scriptagent must be running and registered, with test* script/process rows present."
}
