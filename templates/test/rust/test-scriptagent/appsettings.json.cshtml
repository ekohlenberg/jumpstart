{
  "appsettings": {
    "loglevel": "debug",
    "namespace": "@Model.Name"
  },
  "_note": "Live DB connection is read from ~/.@(Model.Name).json by the common Config loader. Run with --init to (re)create the test workflows; a scriptagent must be running and registered (Agent/Online)."
}
