CREATE SEQUENCE @(Model.SchemaName + "." + Model.TableName)_identity AS BIGINT START WITH 1000 INCREMENT BY 1;
GRANT USAGE, SELECT ON SEQUENCE @(Model.SchemaName + "." + Model.TableName)_identity TO @Model.Namespace;
