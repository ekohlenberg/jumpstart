
-- Insert parent and child navigation menu items
DECLARE @@nav_id BIGINT;
@foreach( var navMenuParent in Model.NavMenus.Keys)
{
    var ordinal = 0;
    @foreach( var navMenuParentKey in Model.NavMenus.Keys)
    {
        if (navMenuParentKey == navMenuParent)
        {
            break;
        }
        ordinal++;
    }
    <text>
-- Insert parent menu item
SET @@nav_id = NEXT VALUE FOR core.nav_menu_identity;
INSERT INTO core.nav_menu(id, txn_id, parent_id, ordinal, name, link, is_active, created_by, last_updated, last_updated_by)
VALUES (@@nav_id, @@nav_id, 0, @ordinal, '@navMenuParent', '', 1, SYSTEM_USER, GETDATE(), SYSTEM_USER);

-- Insert child menu items for @navMenuParent
    </text>
    var navMenuChildList = Model.NavMenus[navMenuParent];
    @foreach( var navChild in navMenuChildList)
    {
        var childOrdinal = 0;
        @foreach( var child in navMenuChildList)
        {
            if (child == navChild)
            {
                break;
            }
            childOrdinal++;
        }
        <text>
SET @@nav_id = NEXT VALUE FOR core.nav_menu_identity;
INSERT INTO core.nav_menu(id, txn_id, parent_id, ordinal, name, link, is_active, created_by, last_updated, last_updated_by)
VALUES (@@nav_id, @@nav_id, (SELECT id FROM core.nav_menu WHERE parent_id = 0 AND name = '@navMenuParent' AND is_active = 1), @childOrdinal, '@navChild.Label', '/@navChild.Uri', 1, SYSTEM_USER, GETDATE(), SYSTEM_USER);
        </text>
    }
}
